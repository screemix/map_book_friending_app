services:
  backend:
    build:
      dockerfile: Dockerfile
      context: backend/.
    #    env_file:
    #      - .env
    ports:
      - "8000:8000"
  frontend:
    #    image: frodan/map_front
    build:
      dockerfile: Dockerfile
      context: frontend/.
    ports:
      - "3000:3000"